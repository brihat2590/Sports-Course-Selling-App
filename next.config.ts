import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['images.unsplash.com','imgs.search.brave.com','www.instagram.com']
     // Add the domain here
  },
  /* config options here */
};

export default nextConfig;
